<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= service.title %> - Service Details</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Open+Sans:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    .details-container {
      max-width: 800px;
      margin: 40px auto;
      padding: 20px;
    }
    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      color: var(--primary);
      font-weight: 600;
      text-decoration: none;
      margin-bottom: 25px;
      transition: color 0.3s ease;
    }
    .back-link:hover {
      color: #1B5E20;
    }
    .detail-section {
      margin-bottom: 30px;
      padding-bottom: 20px;
      border-bottom: 1px solid #ECEFF1;
    }
    .detail-section:last-child {
      border-bottom: none;
    }
    .section-title {
      font-size: 1.25rem;
      font-weight: 700;
      color: #1B5E20;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .section-content {
      font-size: 0.95rem;
      color: #37474F;
      line-height: 1.7;
    }
    .bullet-list {
      margin: 0;
      padding-left: 20px;
    }
    .bullet-list li {
      margin-bottom: 8px;
    }
  </style>
</head>
<body style="background-color: #F4F6F7;">

  <!-- Header -->
  <header class="header" style="background: white; box-shadow: 0 2px 8px rgba(0,0,0,0.05);">
    <nav class="navbar">
      <a href="/" class="logo">
        <div class="logo-icon">
          <i class="fas fa-landmark"></i>
        </div>
        <span class="logo-text">Smart Grama Panchayat</span>
      </a>
      <ul class="nav-links">
        <li><a href="/home"><i class="fas fa-home"></i> Home</a></li>
        <li><a href="/services" class="active"><i class="fas fa-handshake"></i> Services</a></li>
        <% if (session.user) { %>
          <li><a href="/user-dashboard"><i class="fas fa-chart-line"></i> Dashboard</a></li>
          <li><a href="/user-logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
        <% } else if (session.admin) { %>
          <li><a href="/admin-dashboard"><i class="fas fa-chart-line"></i> Dashboard</a></li>
          <li><a href="/admin-logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
        <% } else { %>
          <li><a href="/user-login"><i class="fas fa-user"></i> Citizen Portal</a></li>
          <li><a href="/admin-login"><i class="fas fa-user-shield"></i> Admin</a></li>
        <% } %>
      </ul>
    </nav>
  </header>

  <main class="details-container">
    <a href="javascript:history.back()" class="back-link" id="serviceBackLink">
      <i class="fas fa-arrow-left"></i> Go Back
    </a>

    <div class="card" style="padding: 40px; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.04);">
      <div class="d-flex justify-between align-items-center mb-4 flex-wrap" style="border-bottom: 2px solid #E8F5E9; padding-bottom: 20px;">
        <div>
          <span class="status-badge <%= service.status === 'Active' ? 'paid' : 'inactive' %>" style="margin-bottom: 8px; display: inline-block;"><%= service.status %></span>
          <h1 style="color: #1B5E20; font-weight: 700; font-family: 'Poppins', sans-serif; margin: 0;"><%= service.title %></h1>
        </div>
        <div style="font-size: 3rem; color: #2E7D32;">
          <i class="fas fa-<%= service.icon || 'cogs' %>"></i>
        </div>
      </div>

      <!-- Description -->
      <div class="detail-section">
        <div class="section-title"><i class="fas fa-info-circle"></i> Service Description</div>
        <div class="section-content"><%= service.description %></div>
      </div>

      <!-- Required Documents -->
      <div class="detail-section">
        <div class="section-title"><i class="fas fa-file-alt"></i> Required Documents</div>
        <div class="section-content">
          <% if (service.required_documents) { %>
            <ul class="bullet-list">
              <% service.required_documents.split(',').forEach(function(doc) { %>
                <li><i class="fas fa-check text-success" style="margin-right: 8px;"></i> <%= doc.trim() %></li>
              <% }); %>
            </ul>
          <% } else { %>
            <p>Identity proof (Aadhaar or Voter ID Card), Address Proof (Electricity bill or utility bill), Application form signed by Ward Member.</p>
          <% } %>
        </div>
      </div>

      <!-- Processing Time -->
      <div class="detail-section">
        <div class="section-title"><i class="fas fa-hourglass-half"></i> Processing Time</div>
        <div class="section-content" style="font-weight: 600; color: #E65100;">
          <i class="far fa-clock"></i> <%= service.processing_time || '7-10 Working Days' %>
        </div>
      </div>

      <!-- Office Timings -->
      <div class="detail-section">
        <div class="section-title"><i class="fas fa-calendar-alt"></i> Office Submission Timings</div>
        <div class="section-content">
          <%= service.office_timings || '10:00 AM to 02:00 PM (Monday to Friday)' %>
        </div>
      </div>

      <!-- Contact Details -->
      <div class="detail-section" style="background-color: #E8F5E9; padding: 20px; border-radius: 12px; border: 1px solid #C8E6C9; border-left: 5px solid #2E7D32;">
        <div class="section-title" style="margin-bottom: 6px;"><i class="fas fa-phone-alt"></i> Contact Department</div>
        <div class="section-content" style="font-weight: 500;">
          <%= service.contact_details || 'Panchayat Helpdesk - Contact: +91 80 2843 1234' %>
        </div>
      </div>

    </div>
  </main>

  <!-- Footer -->
  <footer class="footer" style="margin-top: 40px;">
    <div class="footer-content">
      <div class="footer-logo">Smart Grama Panchayat</div>
      <p>Digitizing Rural Governance | All Rights Reserved</p>
    </div>
  </footer>

</body>
</html>
